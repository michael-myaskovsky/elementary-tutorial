version: 2

models:
  - name: marketing_ads
    description: 'Unified view of marketing ads data from various sources'
    columns:
      - name: id
        description: 'The unique identifier for the ad'
        tests:
          - unique
          - not_null
    meta:
      owner: 'Michael'