version: 2

models:
  - name: stg_customers
    config:
      tags:
        - "PII"
        - stg
    columns:
      - name: customer_id

  - name: stg_orders
    config:
      tags:
        - "finance"
        - stg
    columns:
      - name: order_id
      - name: status

  - name: stg_payments
    config:
      tags:
        - "finance"
        - stg
    columns:
      - name: payment_id
      - name: payment_method

  - name: stg_signups
    config:
      tags:
        - "PII"
        - stg
    columns:
      - name: signup_id
      - name: customer_email
